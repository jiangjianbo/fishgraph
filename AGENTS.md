# fishgraph

## 核心原则

### 日志记录

- 每次完成一个任务之后，都需要记录`操作日志.md`文件，把新日志添加到文档尾部

### 语言偏好

- **输出语言**：优先使用中文
- **代码注释**：优先使用中文
- **运行时输出**：只能使用英文（日志、printf等）

## 需求与边界

- 从原始需求和问题本质出发，不机械套用惯例或模板。
- 需求存在歧义或关键信息缺失时，不得自行脑补关键假设，应先澄清。
- 严格区分【事实】【推测】【未知】，禁止编造不存在的接口、文件、数据或项目情况。
- 严格控制需求范围；未经明确要求，不主动增加功能、重构、清理技术债务或全局优化。
- 发现更好的实现路径时，可以提出建议，但不得擅自改变用户要求。
- 高风险、破坏性或重大架构变更必须先说明影响和风险，并获得确认。

## 编程基本原则

以下原则适用于所有代码、脚本、配置、测试和重构任务。

### 意图与实现分离

- 上层流程表达“做什么”，底层函数/模块负责“怎么做”。
- 主流程应保持语义化，不应堆叠具体实现细节。

### 职责与关注点分离

- 函数、类、模块应具有清晰的主要职责。
- 不同关注点应保持独立，不因实现方便而混杂。
- 新增逻辑应放在其职责真正所属的位置，而不是当前最容易修改的位置。

### 封装与复用

- 复杂逻辑应封装在合适的函数、类或模块中。
- 优先复用已有能力，不重复实现已有逻辑。
- 不通过复制代码或增加特殊分支解决本应通过抽象解决的问题。

### 简单与结构

- 优先选择满足当前需求的最简单可靠方案，不提前设计未知需求。
- 局部修改不得破坏已有职责边界、依赖关系和整体结构。
- 当代码不断增加判断、分支和补丁时，应停止局部修补，重新检查设计和根因。

### 显式与可维护

- 重要依赖、错误和前置条件应尽可能显式表达，不依赖隐式状态。
- 错误不得被静默吞掉或通过默认值掩盖。
- 会随环境变化的值不得无理由硬编码，应使用配置、参数或变量。

## 开发执行

- 动手前先理解现有代码、相关模块、依赖关系和成功标准。
- 优先修改最小范围，避免无关改动。
- 修改前先寻找已有实现，避免重复造轮子。
- 遇到问题优先寻找根因，不通过连续打补丁掩盖问题。
- 完成后同时验证“功能是否正确”和“结构是否合理”。
- 遵循项目已有架构、技术选型、代码风格和目录约定，不强行套用外部规范。

## 测试原则

- 测试失败时采用分而治之的方法，从最小单元逐层定位。
- 优先验证单一功能点，再验证组合功能和完整流程。
- 修复问题后先验证最小范围，再逐步扩大验证范围。
- 不得通过降低断言、跳过测试或修改测试以掩盖实现问题。

## 输出与协作

- 用户指定格式时严格遵守。
- 优先输出结论和影响决策的信息，避免无意义的冗余。
- 涉及比较、评估和方案设计时，优先使用表格、流程图等结构化表达。
- 发现明显问题时直接指出，并给出可行替代方案。
- 不确定的信息明确标注，不将推测当作事实。

## 任务 Skill

- 具体任务的执行方法、SOP、检查清单和领域规则由对应 Skill 定义。
- Skill 不得违反本文件中的全局原则。
- 当任务 Skill 与本文件冲突时，以本文件为准。
